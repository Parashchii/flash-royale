VITE_CLERK_PUBLISHABLE_KEY=
VITE_CONVEX_URL=

# Convex dashboard → Settings → Environment Variables:
# CLERK_JWT_ISSUER_DOMAIN=https://YOUR_INSTANCE.clerk.accounts.dev
#
# Clerk → JWT Templates → New → Convex (name must be "convex")
